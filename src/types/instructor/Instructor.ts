export default interface Instructor {
  createdAt?: string;
  email: string;
  firstName: string;
  id: string;
  isVerified?: boolean;
  lastName: string;
  otp?: string;
  password: string;
  phoneNumber?: string;
  profilePic?: string;
  resume: string;
  updatedAt?: string;
}
